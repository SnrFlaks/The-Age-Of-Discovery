using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    public GameObject cannon;
    void Start()
    {
        // for (int i = 0; i < 100; i++)
        // {
        //     Instantiate(cannon, new Vector2(250 + 0.5f, 245 + 0.5f), Quaternion.identity, gameObject.transform.GetChild(3));
        // }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
